# Competition evidence matrix

The competition asks for a submission form, an open-source repository, and a deployed,
walkthrough-ready site. The product demonstration must cover the nine rows below. “Ready” here
means product behavior, accepted design/copy, tests, documentation, and browser evidence agree on
the local candidate; Andrew's provider and public-release gates remain separate.

| #   | Required demonstration                                           | Judge route and action                                                                                                                                                                        | Domain and trust evidence                                                                                                                                                                               | Focused verification                                                                                                                                                             | Status before final gate                                                                                 |
| --- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 1   | Conditional CFP forms and category routing                       | `/forms` → inspect the workshop-only question; `/submit/aie-nyc-2026-cfp` → switch format; `/submissions` → accept a reviewed proposal and inspect its track                                  | `submission-form.create`, `.update`, `.publish`; `submission.create`, `.submit`; accepted answers map the selected track into the created session                                                       | Conditional-field evaluation, required mapping, invalid answer-shape, submitter boundary, and accepted-session track assertions in `tests/core.test.ts` and `tests/http.test.ts` | Ready; track selection is the explicit category-routing contract                                         |
| 2   | Speaker portal for bios, headshots, slides, and supporting files | `/portal/par_003` → edit public profile, confirm participation, submit text, and upload/download an allowed file                                                                              | Participant-scoped projection; `portal.update-profile`, `participation.set-status`, `requirement.set-status`, `requirement.submit-file`; private R2 object path with type, size, and owner checks       | Ownership, invalid file, lifecycle separation, private upload/download, and projection tests                                                                                     | Ready for seeded demo data; production identity, scanning, and retention remain honest host requirements |
| 3   | Templated reminders plus Gmail/Outlook/iCal invitations          | `/communications` → preview personalization, submit, approve, queue, inspect each frozen message and calendar attachment, then retry a controlled pending/failed row                          | `campaign.create-draft`, `.submit`, `.approve`, `.send`, `.retry-deliveries`, `.record-delivery`; post-commit Cloudflare Email consumer sends the frozen RFC 5545 attachment and records its message ID | Audience/content freezing, suppression, provider create/failure/retry, calendar escaping/folding and base64 attachment, projection redaction, and adapter-contract tests         | Product ready; Andrew owns sender-domain activation and one controlled provider-confirmed smoke receipt  |
| 4   | Multi-round scoring; optional AI assist                          | `/reviews` → advance “The boring parts of trustworthy agents” to Finalist review; `/reviewer/rev_002` and `/reviewer/rev_001` → submit finalist scorecards; `/submissions` → accept           | Ordered evaluation rounds; `review.submit-scorecard`, `review.advance-round`, `review.decide`; threshold, scope, version, idempotency, audit, and final-round acceptance gates                          | `guards review-round progression, final-round acceptance, scope, and duplicate retries`; full browser flow persisted across reload                                               | Ready; optional AI scoring is deliberately omitted rather than simulated                                 |
| 5   | Drag/drop agenda, conflict detection, and multiple views         | `/schedule` → switch list/day/week/track/room, filter, place or drag a session, trigger a conflict, undo, inspect preflight, publish; `/agenda` → compare immutable public release            | `schedule.place-session`, `.move-session`, `.unplace-session`, `.publish`; explicit view modes; draft/private versus immutable/public boundary                                                          | Conflict taxonomy, boundary checks, stale versions, safe undo, publish blockers, immutable-release tests, and desktop/375px browser proof                                        | Ready                                                                                                    |
| 6   | Readiness dashboard with outstanding tasks                       | Keep `/readiness` open, change a task in `/portal/par_003` or organizer detail, then observe the scoped dashboard update within five seconds without a manual reload                          | Derived readiness rows from participant requirements; foreground operator polling every five seconds plus focus refresh; organizer and participant transitions remain distinct                          | Every blocker is accounted for; ownership and invalid-transition tests; query refresh semantics; mobile matrix/detail proof                                                      | Ready with bounded near-real-time refresh; WebSockets remain an optional latency optimization            |
| 7   | One-way Accelevents integration                                  | `/integrations` → inspect preflight, stage the latest published release into a controlled event, observe speaker-first create/update results, retry any failed rows, and inspect provider IDs | `accelevents.prepare-export`, `.retry-export`, `.record-result`; authenticated post-commit consumer; official create/update endpoints; latest immutable release; provider-ID reuse across releases      | Mapping, relationship resolution, API header/method/body adapter tests, missing-release blockers, duplicate staging, retries, terminal evidence, and projection redaction        | Product ready; Andrew owns Enterprise-key activation and one controlled provider-confirmed smoke receipt |
| 8   | Portal resources/wiki and HTML embeds                            | `/resources` → save a draft guide, reject an unsafe HTML card, publish a safe card; `/portal/par_003` → inspect participant projection                                                        | `portal-resource.save`; event scoping, versions, staff scope, published-only participant projection; attribute-free static tags in an empty iframe sandbox                                              | Staff-scope, safe/published path, active/remote-content rejection, and projection tests; browser unsafe-script rejection                                                         | Ready                                                                                                    |
| 9   | Embeddable mobile speaker gallery and schedule itinerary         | `/embed/speakers` → search a speaker; `/embed/itinerary` → save sessions, reload, and switch to “My itinerary”                                                                                | Read-only public projection from latest immutable schedule release; itinerary IDs stay in local device storage and never enter workspace state                                                          | Surface-routing tests; browser search and persistence proof at desktop and 375px; no private records in public projection                                                        | Ready; final host CSP/framing smoke test remains Andrew's release gate                                   |

## Bonus posture

| Bonus                | Honest position                                                                                                                          |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Cloudflare           | Strong: one Worker composes the React client, API, SQLite-backed Durable Object state, and private R2 uploads.                           |
| API                  | Strong: event-scoped reads, named writes, operation manifest, versions, idempotency, audit events, export, calendar, and health routes.  |
| Speed                | Production bundles build successfully; final walkthrough should show navigation and mutation latency, not quote an unmeasured benchmark. |
| Airtable persistence | Not claimed. The repository contains a documented reconciliation design and tested comparison primitive, not a delivered runtime mirror. |
| Forge                | Not pursued. Splitting the contributor path is not worth a tiny bonus before the golden path is final.                                   |

## Final external gates

- The repository must be public and readable signed out.
- The deployed `programkit.dev` site must have valid TLS and pass the final host/CSP checks.
- Email sender verification and the Accelevents Enterprise secret remain outside the repository;
  Andrew must retain controlled provider-confirmed smoke receipts before recording.
- The submission form and walkthrough URL must be verified after publication.
